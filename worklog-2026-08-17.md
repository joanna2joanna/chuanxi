# 川西自驾路书卡片 工作记录 2026-08-17

## 完成内容

**1. 卡片视觉修改（gen_cards.py 与重生成）**
- 顶部经幡色条：原与底色融在一起的白色条改为经幡蓝（#2f6fd0），五色与底区区分清楚
- 修复第二章起顶部灰块：根因是 `.card__inner` 内容接近满页时，`.flag` 作为可收缩 flex 子项被压成 0 高度，卡片整体上移、顶部露出 body 灰色背景。给 `.flag` 和 `.card__foot` 加 `flex-shrink:0` 修复
- 底部文字改为「李大妈自驾路书」
- 16/17 页字号变小：`.card ul` 列表补 `font-size:29px;line-height:1.92`，与正文统一
- 开篇「六把钥匙」类标题由弱黄色改为朱砂深（#a83a30），全系列以此类推

**2. 孤字换行修复**
- 用 `white-space:nowrap` 的 span 粘住关键短语，消除行尾孤字（不改文字内容、不影响分页），共 9 处：
  出发前读一遍、围栏别随便进、按天十元左右、高德的新能源模式、藏族金工博物馆、房子就是环境的说明书、谁家/办完成人礼才成家、跳过导读读正文
- 验证「书。」「家。」已解决：合并后该章在**第 13 页**，两处均为完整短语独占一行，无孤字

**3. 分页重排与合并**
- 分页逻辑从字数估算重写为按块高度估算（block_height + 高度贪心），h2 强制翻页、h3 用前瞻
- 正文搜索可用高度 H=1285，使总页数控制在 18（1+正文+2附录）
- 孤页合并：第 13 页（印经院至今没通电，单段 253px）并入第 12 页，总页数 18 → 17

**4. 文件清理**
- 删除上一批重复命名的截图（card-0*.png 批次），统一为 card-N.png 无前导零，共 17 张

## 待用户人工检查
- 17 张卡片的 PNG 截图（cards/card-1.png 至 card-17.png），本人不做自动截图验证
